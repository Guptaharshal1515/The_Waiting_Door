## Packages
howler | For managing ambient audio and sound effects easily
framer-motion | For smooth UI transitions (start screen, end screen)

## Notes
- The game uses HTML5 Canvas API for rendering.
- Game loop runs independently of React render cycle for performance.
- Audio (drone/hum) is generated or played via Howler.
- Dark mode is enforced globally.
